import React from "react";

const FeaturesTitle = ({ title }) => {
  return (
    <h3 className="text-center text-4xl font-semibold capitalize text-blue mb-5">
      {title}
    </h3>
  );
};

export default FeaturesTitle;
